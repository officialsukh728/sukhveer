import 'dart:io';

/// mai eh program plus krn ala bna rya

void main() {
  // int x=3055, y=4550;
  // print(x * y);
  // print(x+y);
  // print(x/y);
  // print(x~/y);
  // // print(x-y);
  // int x, z, y;
  // x = 10;
  // y = 20;
  // z = x + y;
  // print("z is value $z");

  ///find using all dellafiar operalar
 var x=152;
var y= 19;
 (x < y) ? print("x is greater then y") : print("x is not greater then y");


//
//   x = 10;
//   y = 20;
//    z = x + y;
//    stdout.write("Sum of numbers => $z \n");
//
//
//   /// from user data
//    stdout.write("Enter X Value = ");
//   x = int.tryParse(stdin.readLineSync() ?? "0")??0;
//   stdout.write("Enter Y Value = ");
//   y = int.tryParse(stdin.readLineSync() ?? "0")??0;
//   // z = x + y;
//   stdout.write("Sum of numbers => $z \n");
}
