import 'dart:io';

void main() {
  /// from user data
  // stdout.write("Enter X Value = ");
  // x = int.tryParse(stdin.readLineSync() ?? "0")??0;
  // stdout.write("Enter Y Value = ");
  // y = int.tryParse(stdin.readLineSync() ?? "0")??0;
  // z = x + y;
  // stdout.write("Sum of numbers => $z \n");
  stdout.write("enter X valve = ");
  // var x = int.tryParse(stdin.readLineSync() ?? "0") ?? 0;
  // stdout.write("Enter y Value = ");
  // var y = int.tryParse(stdin.readLineSync() ?? "0") ?? 0;
  // var z = x + y;
  // stdout.write("sum of numbers => $z \n");
  // var x = int.tryParse(stdin.readLineSync() ?? "0") ?? 0;
  // stdout.write("Enter y Value = ");
  // var y = int.tryParse(stdin.readLineSync() ?? "0") ?? 0;
  // var z = x - y;
  // stdout.write("sum of numbers => $z \n");
  // var x = int.tryParse(stdin.readLineSync() ?? "0") ?? 0;
  // stdout.write("Enter y Value = ");
  // var y = int.tryParse(stdin.readLineSync() ?? "0") ?? 0;
  // var z = x * y;
  // stdout.write("sum of numbers => $z \n");
  var x = int.tryParse(stdin.readLineSync() ?? "0") ?? 0;
  stdout.write("Enter y Value = ");
  var y = int.tryParse(stdin.readLineSync() ?? "0") ?? 0;
  var z = x / y;
  stdout.write("sum of numbers => $z \n");


}
